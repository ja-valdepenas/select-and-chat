<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Android 11+ hides other packages by default. Naming the two WhatsApp packages
      explicitly is what lets setPackage() and the installed-check work, while avoiding
      QUERY_ALL_PACKAGES, which would trigger a Play Store declaration form.
      This app declares no permissions at all.
    -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SelectAndChat">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The text-selection toolbar item. android:label is the text the user sees there,
          so it is the action name, not the app name.
        -->
        <activity
            android:name=".ui.ProcessTextActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/toolbar_action_label"
            android:taskAffinity=""
            android:theme="@style/Theme.SelectAndChat.Translucent">

            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Same logic, reached from the share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>

</manifest>
